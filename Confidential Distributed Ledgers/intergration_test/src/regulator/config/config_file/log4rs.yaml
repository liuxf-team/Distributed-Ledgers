refresh_rate: 30 seconds

appenders:
  file:
    kind: file
    path: "src/regulator/logs/regulator.log"
    encoder:
      pattern: "{d} {l} {M} - {m}{n}"
    append: false

root:
  level: info
  appenders:
    - file

